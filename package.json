{
  "name": "ourpop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "at-merge": "git checkout dev && git add . && git commit -m 'at-commit at-merge' && git checkout main && git merge dev && git push && git checkout dev"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "f-analytics": "^1.1.0",
    "firebase": "^10.14.0",
    "next": "14.2.14",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
